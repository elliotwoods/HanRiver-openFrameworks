this class has its own thread
	vector of ofPtr<ofPixels>
	vector of bool (found)
	has a calibration object
	in its own time it adds all the found frames so far one at a time

	this is for its own intrinsics

for extrinsics do elsewhere with mathcing pairs